<%= form_for @message do |f|%>

	<%= f.label :recipient_id %>
	<%= f.select :recipient_id, @users.map { |u| [u.email, u.id] } %>

	<%= f.label :body %>
	<%= f.text_field :body %>
	<%= f.submit %>

<%end%>